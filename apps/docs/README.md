# GET /docs

Serves interactive Swagger UI for the InboxPilot API.

The OpenAPI spec from `swagger.yaml` is embedded at build time. The domain is injected via the `INBOXPILOT_DOMAIN` environment variable during the build.
